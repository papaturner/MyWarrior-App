<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_dark_gradient">

    <!-- title -->
    <TextView
        android:id="@+id/tvWorldTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="⚔️ WORLD 1: CURSED REALM ⚔️"
        android:textColor="@color/text_gold"
        android:textSize="22sp"
        android:textStyle="bold|italic"
        android:gravity="center"
        android:padding="20dp"
        app:layout_constraintTop_toTopOf="parent"/>

    <TextView
        android:id="@+id/tvWorldDesc"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Choose a battlefield to test your strength"
        android:textColor="@color/text_secondary"
        android:textSize="14sp"
        android:gravity="center"
        android:layout_marginBottom="16dp"
        app:layout_constraintTop_toBottomOf="@id/tvWorldTitle"/>

    <!-- locations -->
    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_marginHorizontal="20dp"
        app:layout_constraintTop_toBottomOf="@id/tvWorldDesc"
        app:layout_constraintBottom_toTopOf="@id/bottomNav">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <!-- level 1 -->
            <Button
                android:id="@+id/btnLevel1"
                android:layout_width="match_parent"
                android:layout_height="80dp"
                android:text="🏰 1. Castle Ruins\nSlimes &amp; Goblins  •  Easy"
                android:textColor="@color/text_primary"
                android:textSize="15sp"
                android:textStyle="bold"
                android:background="@drawable/card_background"
                android:gravity="center_vertical|start"
                android:paddingStart="20dp"
                android:layout_marginBottom="12dp"/>

            <!-- level 2 -->
            <Button
                android:id="@+id/btnLevel2"
                android:layout_width="match_parent"
                android:layout_height="80dp"
                android:text="🌲 2. Dark Forest\nWolves &amp; Orcs  •  Medium"
                android:textColor="@color/text_primary"
                android:textSize="15sp"
                android:textStyle="bold"
                android:background="@drawable/card_background"
                android:gravity="center_vertical|start"
                android:paddingStart="20dp"
                android:layout_marginBottom="12dp"/>

            <!-- level 3 -->
            <Button
                android:id="@+id/btnLevel3"
                android:layout_width="match_parent"
                android:layout_height="80dp"
                android:text="🌉 3. Bridge of Trials\nTrolls &amp; Knights  •  Hard"
                android:textColor="@color/text_primary"
                android:textSize="15sp"
                android:textStyle="bold"
                android:background="@drawable/card_background"
                android:gravity="center_vertical|start"
                android:paddingStart="20dp"
                android:layout_marginBottom="12dp"/>

            <!-- level 4 -->
            <Button
                android:id="@+id/btnLevel4"
                android:layout_width="match_parent"
                android:layout_height="80dp"
                android:text="🐉 4. Dragon's Peak\nBOSS BATTLE  •  Extreme"
                android:textColor="@color/element_fire"
                android:textSize="15sp"
                android:textStyle="bold"
                android:background="@drawable/quest_card_background"
                android:gravity="center_vertical|start"
                android:paddingStart="20dp"
                android:layout_marginBottom="16dp"/>
        </LinearLayout>
    </ScrollView>

    <!-- bottom nav -->
    <LinearLayout
        android:id="@+id/bottomNav"
        android:layout_width="match_parent"
        android:layout_height="64dp"
        android:orientation="horizontal"
        android:background="@drawable/bg_bottom_nav"
        android:gravity="center_vertical"
        app:layout_constraintBottom_toBottomOf="parent">

        <ImageButton
            android:id="@+id/btnHome"
            android:layout_width="0dp"
            android:layout_height="48dp"
            android:layout_weight="1"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@android:drawable/ic_menu_today"
            android:contentDescription="Home"
            app:tint="@color/text_secondary"/>

        <ImageButton
            android:id="@+id/btnCharacter"
            android:layout_width="0dp"
            android:layout_height="48dp"
            android:layout_weight="1"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@android:drawable/ic_menu_myplaces"
            android:contentDescription="Character"
            app:tint="@color/text_secondary"/>

        <ImageButton
            android:id="@+id/btnBattle"
            android:layout_width="0dp"
            android:layout_height="48dp"
            android:layout_weight="1"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@android:drawable/ic_media_play"
            android:contentDescription="Battle"
            app:tint="@color/brand_yellow"/>

        <ImageButton
            android:id="@+id/btnSettings"
            android:layout_width="0dp"
            android:layout_height="48dp"
            android:layout_weight="1"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@android:drawable/ic_menu_preferences"
            android:contentDescription="Settings"
            app:tint="@color/text_secondary"/>
    </LinearLayout>
</androidx.constraintlayout.widget.ConstraintLayout>
